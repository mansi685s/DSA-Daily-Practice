/*
Problem-19.Remove Nth Nodefrom End of List
Difficulty-Medium
Platform-Leetcode
Approach(Two Pointer, LinkedList)-
1.Update temp Pointer from i=1 to same as n.
2.Update both pointer  temp and ptr till temp.next equal to null.
3. Removing Head: If after creating the gap, temp is already null, it means we need to remove the first node of the list. We return head.next.
4.Change the next pointer of the temp node to skip the target node: ptr.next = ptr.next.next
*/

class Solution {
    public ListNode removeNthFromEnd(ListNode head, int n) {
       if(head.next==null){
        return null;
       }
       
        int i=1;
        ListNode temp=head;
        ListNode ptr=head;
        while(i<=n){
            temp=temp.next;
            i++;
        } if (temp== null) return head.next;
        while(temp.next!=null){
            ptr=ptr.next;
            temp=temp.next;
        }
        if(ptr.next!=null){
         ptr.next=ptr.next.next;
        }
     return head;
    }
}
