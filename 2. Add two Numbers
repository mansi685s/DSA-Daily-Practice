/*
Problem- 2.Add two Numbers
Platform-Leetcode
Difficulty-Medium
Frequency-1ms
Approach-Iterative traversal (Two Pointer Technique)
Create a dummy node to store the result list.
Traverse both linked lists simultaneously using iteration.
At each step:
Add values of current nodes and previous carry.
Store (sum % 10) as the new node.
Update carry = sum / 10.
Move pointers of both lists forward.
Continue until both lists become null.
If a carry remains at the end, add one extra node.
Return dummy.next as the final result.
Solution-
*/

class Solution {
    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {      
      ListNode dummy=new ListNode();
      ListNode res=dummy;
      int sum=0,carry=0;
      while(l1!=null||l2!=null){
       sum=carry;
        if(l1!=null){
           sum+=l1.val;
           l1=l1.next;
        }
        if(l2!=null){
            sum+=l2.val;
            l2=l2.next;
        }
        carry=sum/10;
        res.next=new ListNode(sum%10);
        res=res.next;
      }
      if (carry!=0){
            res.next=new ListNode(carry);
        }
      return dummy.next;
    }
}
