/*Problem- 82.Remove duplicates from Sorted List II(LinkedList)
Platform-Leetcode
Level- Easy

Approach(Two Pointer)-
Since the linked list is sorted, duplicate values appear consecutively.
We use a dummy node to safely handle cases where the head itself is duplicated.
prev → last confirmed non-duplicate node
curr → traverses the list */

//Solution-
 class Solution {
    public ListNode deleteDuplicates(ListNode head) {
        ListNode dummy=new ListNode(0);
        dummy.next=head;
        ListNode prev=dummy;
        ListNode curr=dummy.next;
       while(curr!=null){
        if(curr.next!=null&&curr.val==curr.next.val){
            while(curr.next!=null&&curr.val==curr.next.val){
                curr=curr.next;
            }
             curr = curr.next;   
                prev.next = curr; 
        }
        else{
           prev=curr;
           curr=curr.next;
        }
       
       }
       return dummy.next;
    }
}
